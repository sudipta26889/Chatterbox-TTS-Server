# requirements-nvidia.txt (For NVIDIA GPU Installation with CUDA 12.8 - Blackwell sm_120 support)
# This file ensures a compatible set of CUDA-enabled torch packages are installed.
# NOTE: This file is split - torch installed separately in Dockerfile to avoid dependency conflicts

# --- Core Application Dependencies (NON-TORCH) ---

# Core Web Framework
fastapi
uvicorn[standard]

# Chatterbox dependencies (installed with --no-deps to avoid torch conflict)
resemble-perth
transformers
diffusers
omegaconf
einops
s3tokenizer
conformer
resampy

# Other ML & Audio Libraries
numpy>=1.26.0,<3.0.0  # More flexible to avoid conflicts
soundfile  # Requires libsndfile system library
librosa
safetensors
descript-audio-codec

# Configuration & Utilities
PyYAML
python-multipart
requests
Jinja2
watchdog
aiofiles
unidecode
inflect
tqdm
hf_transfer                     # Speed up file transfers

# Audio Post-processing
pydub
audiotsm
praat-parselmouth
